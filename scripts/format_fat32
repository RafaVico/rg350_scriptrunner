#!/bin/sh
# title=Format SD Card (FAT32)
# desc=Format the external sd card [fat32]. Save the content before the process, sd card will be erased.

echo "Formating deactivated"
echo "mkfs -t vfat /dev/mmcblk1p1"
echo "Finished formating"

